# 论文工作区

`paper/` 和 `docs/` 都会出现文字材料，但两者职责不同：

- `paper/`：只放直接服务论文成稿的材料，例如正文草稿、章节版本、最终会进入论文的图表和引用核查记录。
- `docs/`：放项目过程文档和辅助说明，例如 AGENTS、技能规划、运行说明、阶段整理规则等。

如果一个文件的主要用途是“最后要写进论文或直接支撑论文成稿”，优先放在 `paper/`。
如果一个文件的主要用途是“帮助项目协作、运行、整理或说明规则”，优先放在 `docs/`。
